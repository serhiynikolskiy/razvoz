{% extends '::base_admin.html.twig' %}

{% block body -%}
    <h1>Roles</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ entity.isactive }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        
   {{ form(delete_form) }}

{% endblock %}
